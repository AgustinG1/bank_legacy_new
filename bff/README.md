# Banco XYZ - Backend for Frontend

La documentación de S5 está unificada en el [README principal](../README.md): propuesta técnica, módulos, endpoints, autenticación, HTTPS, configuración y pruebas.

Los comandos documentados se ejecutan desde la carpeta principal `bank-batch`.
